# ClawdBot 项目记忆文档

> 本文档用于 AI 助手在跨会话间保持上下文。每次新会话请先读取此文件。

---

## 一、项目概述

- **项目名**：dingtalk-task-tracker（ClawdBot）
- **用途**：钉钉部门工作事项自动化跟踪、催办、汇报系统
- **技术栈**：Node.js + Express + Chart.js(chartjs-node-canvas) + AntV G2Plot(CDN) + 阿里云OSS
- **数据源**：钉钉在线文档（通过API读取），解析为结构化任务数据
- **输出渠道**：钉钉群机器人（Webhook），支持 Markdown / ActionCard 消息类型
- **AI引擎**：Gemini → Kimi → 规则引擎（三级降级）

---

## 二、用户（Peach）的核心需求与不满

### 已明确表达的需求：

1. **可视化看板显示乱码** → 已修复（原因：uploadReport 二次包装HTML）
2. **图表太简陋/不好看** → 用户多次强调，当前 Chart.js 暗色主题仍不满意
3. **不是钉钉原生嵌入式风格** → 用户反复要求"钉钉原生的那种嵌入到图表"、"带滚轮可滚动的那种"
4. **"个人负荷"不是考量维度** → 已去掉，换成「部门完成率」和「逾期天数分布」
5. **报送规则设计不对** → 用户说"重在总结"，而非催办。当前系统偏重催办（催办提醒、逾期预警），缺少总结性报告
6. **交互式看板需要重新调整** → 用户对当前 G2Plot 暗色驾驶舱的效果仍不满意
7. **要看全部消息样式** → 已实现 --preview-all，但用户仍觉得效果不好
8. **需要多种报送规则方案** → 用户要求看到完整的报送体系设计

### 关键理解（用户隐含需求）：

- 用户是**管理者视角**，需要的是**工作总结汇报**，不是催办工具
- "总结"比"催办"重要 —— 系统应该像一个智能秘书，帮领导**了解全局进展**
- 用户希望钉钉群里直接看到的消息就足够好看，不一定需要跳转到外部HTML
- 用户对"图表嵌入钉钉"有很高期望，但钉钉Markdown的能力有限（不支持HTML表格、不支持原生图表）
- 用户可能需要考虑钉钉「互动卡片」（模板卡片）来实现真正的原生嵌入效果

---

## 三、当前系统架构

### 消息类型（5种）：

| 编号 | 名称 | 命令 | 定时 | 格式 |
|------|------|------|------|------|
| 1 | 晨报焦点 | --morning | 10:00 | Markdown |
| 2 | 部门看板 | --dashboard | 18:00 | ActionCard + HTML看板链接 |
| 3 | 事项明细表 | --detail | 按需 | Markdown 长列表 |
| 4 | 单项催办 | --urgent | 14:00 | Markdown x N条 |
| 5 | 周五回顾 | --weekly | 周五18:00 | Markdown |

### 图表生成：

- **PNG图片**：chart-generator.js 用 chartjs-node-canvas 本地渲染 → 上传OSS → 嵌入钉钉消息
- **交互式HTML**：dashboard-html.js 用 AntV G2Plot CDN → 上传OSS → ActionCard按钮跳转
- 当前生成4张PNG：部门分布、状态环形、完成率排行、异常信号
- 交互式看板6个图表：上述4个 + 逾期天数分布 + 异常明细表

### 定时规则（scheduler.js）：

- 10:00 晨报焦点
- 14:00 催办提醒
- 18:00 部门看板（周五→周回顾）

---

## 四、待解决问题（下次会话优先处理）

### P0 - 必须解决：

1. **重新定义报送规则** — 从"催办导向"改为"总结汇报导向"
   - 用户说"重在总结"
   - 需要设计新的报送体系：日总结、周总结、月总结
   - 总结应包含：本日/本周进展概览、完成事项列表、新增事项、风险预警、下一步计划
   - 催办应降为辅助功能，不是主线

2. **图表品质** — 用户多次说"简陋"、"不好看"
   - 方案A：评估使用更好的图表库（ECharts暗色主题？Highcharts？）
   - 方案B：使用 Puppeteer 渲染 G2Plot 为高质量PNG
   - 方案C：放弃PNG图片嵌入，改用钉钉「互动卡片」实现原生体验
   - 需要先做POC让用户选择方向

3. **钉钉原生嵌入** — 用户反复提到"钉钉原生"
   - 调研钉钉「互动卡片」（模板卡片）能力
   - 调研钉钉「酷应用」能力
   - 如果都不行，至少让markdown排版更精致、图片更高清

### P1 - 重要改进：

4. **交互式看板重设计** — 当前效果用户不满意
5. **消息内容调整** — 去掉催办为主的语气，改为客观总结
6. **让用户看到全部样式** — 用户说"只看到一条"，需要确保 --preview-all 在用户机器上正确运行

---

## 五、用户偏好

- **主题风格**：暗色大屏驾驶舱（用户确认）
- **图表库参考**：用户提到 @ant-design/charts、@antv/g2plot、ECharts
- **前端框架**：项目是纯 Node.js 后端，无前端框架。图表是 server-side 渲染 PNG 或 standalone HTML
- **语言**：中文，消息内容全中文
- **角色**：用户是部门管理者，需要了解下属各部门工作进展
- **关注维度**：部门进展、完成率、异常事项、逾期情况（不关心个人负荷）

---

## 六、技术备忘

### 钉钉消息能力限制：

- Markdown消息：支持标题、粗体、链接、图片、有序/无序列表、引用
- **不支持**：HTML表格、内联CSS、iframe、JavaScript
- ActionCard：支持整体跳转或独立跳转按钮
- 图片嵌入：`![](url)` 只能用公网可访问的URL
- 钉钉「互动卡片」：可创建模板化卡片，支持进度条、标签等原生组件（需在开发者后台配置模板）

### OSS 上传注意：

- dashboard HTML 必须用 `uploadFile(key, buffer, 'text/html; charset=utf-8')` 直接上传
- **不能**用 `uploadReport()`，它会二次包装导致乱码
- 图表PNG用 `uploadFile(key, buffer, 'image/png')`

### 关键文件路径：

- 消息模板：`src/modules/message-templates.js`
- 图表生成：`src/modules/chart-generator.js`
- HTML看板：`src/modules/dashboard-html.js`
- 定时调度：`src/scheduler.js`
- 本地发送：`scripts/local-send.js`
- 任务数据：`data/latest-tasks.json`

---

## 七、会话历史摘要

### 会话 2026-03-18：

1. 用户反馈可视化看板乱码 → 修复OSS上传二次包装bug
2. 用户要求升级图表库 → 从ECharts切换到AntV G2Plot暗色主题
3. 用户要求去掉"个人负荷" → 换成部门完成率 + 逾期天数分布
4. 用户要求看全部消息样式 → 实现 --preview-all 一次发5条
5. 用户说"简陋"、"不好看" → 升级chart-generator暗色主题
6. 用户说"规则不对、重在总结" → **下次会话需要重新设计报送体系**
7. 用户要求生成记忆文档 → 本文档

### 未完成任务（明天继续）：

- [ ] 重新设计报送规则体系（总结导向）
- [ ] 图表品质再次升级（可能需要换方案）
- [ ] 钉钉原生嵌入方案评估
- [ ] 交互式看板重新设计
- [ ] 让用户成功看到全部5种消息样式并确认
